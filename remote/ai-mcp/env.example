# Copy this to .env and fill in values

# Public domain pointing to this server (A/AAAA record)
DOMAIN=mcptest.yourcompany.com

# Basic auth username for protected endpoints (optional, recommended)
BASIC_AUTH_USER=admin

# Caddy needs a bcrypt hash of your password. Generate with:
#   docker run --rm caddy:2 caddy hash-password --plaintext "your_password"
# Paste the hash (starts with $2a$...) here
BASIC_AUTH_HASH=$2a$12$REPLACE_WITH_HASH

# Email for ACME/Let's Encrypt notifications
ACME_EMAIL=devops@yourcompany.com

# ProfitWell API Configuration
PROFITWELL_API_TOKEN=your_private_profitwell_token_here
PROFITWELL_API_BASE=https://api.profitwell.com

# Optional: n8n webhook for metrics (if you set up the n8n workflow)
N8N_METRICS_WEBHOOK_URL=https://your-n8n-host/webhook/your-webhook-id
